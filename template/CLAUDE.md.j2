# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This has not yet been determined. Planning with the user is required.

## Common Commands

- **Run the application**: `uv run simple_v4l2_snapshot`
- **Add dependencies**: `uv add {package}`
- **Remove dependencies**: `uv remove {package}`
- **Install dev dependencies**: `uv sync --dev`
- **Run tests**: `uv run pytest` (when tests are present)
- **Format code**: `uv run ruff format`
- **Lint code**: `uv run ruff check`
- **Type check**: `uv run pyright`

## Project Structure

- The `src` directory is not a Python module - NEVER mention it in imports, ALWAYS start absolute imports with the module name (e.g., `from {{module_name}} import ...`)
- Entry point is defined in pyproject.toml as `{{module_name}}:main`
- Uses Python {{python_version}}+ with type annotations
- Docker deployment available via Dockerfile

## Development Guidelines

- Code formatting: 100 char line length, 2-space indentation
- All imports should be absolute from `{{module_name}}`
- Use `uv run` to execute Python scripts within the project's virtual environment
